# Create a token from https://vercel.com/account/tokens
# The token must be generated from the Vercel account that owns the domain used by local tests (see VERCEL_DOMAIN defined below)
# XXX In our case (at Unly), the token should be generated from a user account that belongs to the Vercel "Unly OSS" team.
VERCEL_TOKEN=

# Domain used for local testing, when running `yarn test`.
VERCEL_DOMAIN=nrn-v2-mst-aptd-gcms-lcz-sty-c1-hfq88g3jt.vercel.app
